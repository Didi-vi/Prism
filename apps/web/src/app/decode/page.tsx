export default function DecodePage() {
  return (
    <main>
      <h1>Decode Transaction</h1>
      {/* TODO: Decoded error card, contract error resolution, context summary, suggested fixes */}
    </main>
  );
}
