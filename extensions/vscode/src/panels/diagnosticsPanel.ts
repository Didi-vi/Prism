// Diagnostics panel — lists recent test failures with decoded errors
import * as vscode from "vscode";

export class DiagnosticsPanel {
  public static readonly viewType = "prism.diagnostics";

  // TODO: Create webview panel showing decoded test failures grouped by category
}
